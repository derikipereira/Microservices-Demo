server:
  port: 8888
spring:
  application:
    name: drk-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/derikipereira/drk-config-server
          default-label: main
          search-paths:
          - 'greeting-service*'